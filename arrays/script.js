const pokemon = ['pikachu', 'bulbasaur','Gyrados','charizard','Golduck']

/* for (const iterator of pokemon) {
    console.log(iterator[0])
}
 */
pokemon.pop()//retira o ultimo elemento da array
pokemon.shift() //retira o primeiro elemento
pokemon.push('charmander') // adiciona um elemento como ultimo
pokemon[0].replace( );
console.log(pokemon)